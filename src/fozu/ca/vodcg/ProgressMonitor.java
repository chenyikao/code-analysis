/**
 * 
 */
package fozu.ca.vodcg;

/**
 * @author Kao, Chen-yi
 *
 */
public class ProgressMonitor {

//	public ProgressMonitor setWorkRemaining(int work, int subTotalWork, SystemElement workHost) {
//		(pm == null || pm.isEmpty() ? "" : pm + "-") 
//		+ Integer.valueOf(work) + "/" + subTotalWork + "[" + workHost + "]")));
//	}
	
}